{
  "name": "slotbound-xhs",
  "version": "1.0.0",
  "private": true,
  "description": "Slotbound 中文竖屏版 — 小红书小工具",
  "scripts": {
    "build": "node tools/build.mjs",
    "check": "node .skill/minitool-zip-builder/scripts/validate.mjs",
    "pack": "node .skill/minitool-zip-builder/scripts/pack.mjs",
    "test": "node tools/smoke.mjs",
    "all": "npm run build && npm run test && npm run check && npm run pack"
  },
  "devDependencies": {
    "@napi-rs/canvas": "^1.0.9",
    "esbuild": "^0.24.0",
    "jsdom": "^30.0.1"
  }
}
